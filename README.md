# LightWebFileBrowser

```sh
$> git clone git@github.com:Dewep/LightWebFileBrowser.git
$> cd LightWebFileBrowser
$> docker build -t lwfb .
$> docker run -p 127.0.0.1:5050:80 -d -v /home/seedbox/transmission:/data/_download --name lwfb_seedbox lwfb
```
